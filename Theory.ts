// Angular is a component-based framework for building scalable app.
// Developed by Google
// AngularJs is old version

// Angular creates Single Page Application(SPA)

// ExpressJs is a framework of NodeJs


/********  Typescript *********/
// A strongly type language
// Used for type safety
// Created by Microsoft
// Superset of JavaScript
// One interesting fact is: Browser cannot run typescript directly, first TS compiles down to JS afterthat this compiles


  // Installing and Creating TS Program
    // Install Node
    // 'npm init' to create a package.json
    // Install TS by 'npm i typescript'
    // For installing TS compiler 'tsc --init


// In TS file if there have some issues/errors output always generates, means tsc never stops, but it can provides some warnings


// Q.) What is Angular-CLI?
// ANS.) Angular-CLI is a command-line interface tool that is used to initialize, develop and maintain Angular applications directly from a command shell  


/**************************** Angular Install *****************************/

// Install Node from ---> 'Node.org'
// Install Angular-CLI globally from command prompt ---> 'npm install -g @angular/cli'
// Create new Angular Project in Vs-Code --> ng new <Project-Name>

// For create component write command in terminal ---> ng generate component <component-name>